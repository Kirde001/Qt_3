еще один поиск филтьтр
дата издания а не год, ввод календарем

#ifndef EDITBOOKDIALOG_H
#define EDITBOOKDIALOG_H

#include <QDialog>
#include <QLineEdit>
#include <QSpinBox>
#include <QDialogButtonBox>
#include <QVBoxLayout>
#include <QLabel>
#include <QDateEdit> // Добавляем

class EditBookDialog : public QDialog
{
    Q_OBJECT

public:
    explicit EditBookDialog(QWidget *parent = nullptr);
    ~EditBookDialog();

    QString getAuthor() const;
    QString getTitle() const;
    QDate getPublishDate() const; // Работаем с QDate
    QString getGenre() const;
    QString getPublisher() const;
    QString getISBN() const;
    int getPageCount() const;


    // Метод для установки данных книги в поля диалога (принимает QDate)
    void setBookData(const QString &author, const QString &title, const QDate &publishDate, const QString &genre,
                     const QString &publisher, const QString &isbn, int pageCount);

private slots:
    void on_buttonBox_accepted();

private:
    QLineEdit *authorLineEdit;
    QLineEdit *titleLineEdit;
    QDateEdit *publishDateEdit; // Используем QDateEdit
    QLineEdit *genreLineEdit;
    QLineEdit *publisherLineEdit;
    QLineEdit *isbnLineEdit;
    QSpinBox *pageCountSpinBox;

    QDialogButtonBox *buttonBox;

    void setupUi();
    bool validateInput();
};

#endif // EDITBOOKDIALOG_H
