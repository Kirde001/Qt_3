еще один поиск филтьтр
дата издания а не год, ввод календарем

#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QStandardItemModel>
#include <QSortFilterProxyModel>
#include <QComboBox>
#include <QDate> // Добавляем

QT_BEGIN_NAMESPACE
namespace Ui { class MainWindow; }
QT_END_NAMESPACE

class EditBookDialog;

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

private slots:
    void addBook();
    void editBook();
    void removeBook();
    void searchBooks(); // Общий поиск
    void searchBooksByDate(); // Слот для поиска по дате
    void importCSV();
    void exportCSV();
    void importJSON();
    void exportJSON();

private:
    Ui::MainWindow *ui;
    QStandardItemModel *model;
    QSortFilterProxyModel *proxyModel;
    void setupModel();
    void setupSearch();
    int getColumnIndex(const QString &columnTitle) const;
};

#endif // MAINWINDOW_H
